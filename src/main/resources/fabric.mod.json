{
  "schemaVersion": 1,
  "id": "clockstopper",
  "version": "${version}",
  "name": "ClockStopper",
  "description": "A fabric mod that stops the game's tick cycle when no players are online.",
  "authors": [],
  "contact": {},
  "license": "MIT",
  "icon": "assets/clockstopper/icon.png",
  "environment": "server",
  "entrypoints": {
    "main": [
      "com.alignedcookie88.clockstopper.ClockStopper"
    ]
  },
  "mixins": [
    "clockstopper.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}"
  }
}
